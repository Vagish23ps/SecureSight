# SecureFlow Environment Configuration

# Optional: GitHub Personal Access Token
# Public repositories can be scanned without a token (rate limit: 60 requests/hour).
# Adding a fine-grained or classic GitHub token raises your rate limit to 5,000 requests/hour.
# Permissions required: public_repo (read-only)
GITHUB_TOKEN=